# ecg-heartbeat-classifier
Machine Learning project for ECG heartbeat classification using MIT-BIH dataset. Includes preprocessing, feature extraction, EDA, and model comparison (Logistic Regression, Random Forest, XGBoost) to classify heartbeats as normal or abnormal with performance evaluation.
